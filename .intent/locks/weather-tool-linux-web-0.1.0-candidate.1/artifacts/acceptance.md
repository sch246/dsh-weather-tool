# Candidate 1 acceptance evidence

The committed source passes `node --check index.mjs`, shell syntax checks for both lifecycle scripts, six Node tests, protocol 0.2 structural validation, `npm pack --dry-run`, staged-diff checks, and a repository scan for local deployment values and common secret or personal-data forms.

After the Web profile switched from its profile-local executable to the linked bundle, the managed service restarted active, served HTTP 200, and the committed entry completed a real current-conditions and three-day-forecast request using profile-local configuration. No real query input or response is retained in this bundle.

This is incomplete acceptance evidence. The linked-package uninstall procedure has not yet been exercised against the live profile.
